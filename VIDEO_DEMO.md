# VIDEO_DEMO.md — сценарий 5-минутного видео-демо (P0; подтверждён переоценкой 2026-08-13: DeepSeek 7 + Kimi 5, оба 9.2/10)

Цель: 5-минутное видео, показывающее реальную работу My OpenCode v2 —
локального AI-агента-программиста на Ollama. Запись экрана (OBS/ShareX)
+ микрофон. Никакого монтажа — один непрерывный дубль.

## Подготовка (до записи)

1. `ollama list` — убедиться, что есть `qwen3:8b` (main) и `nomic-embed-text`.
2. Запуск сервера: `python -X utf8 agent.py` → открыть http://localhost:8765
   (или `scripts/run_tauri.bat` для десктоп-окна — бонус-кадр).
3. Папка демо: создать пустую `E:\video_demo` и разрешить агенту туда писать:
   запустить сервер с `EXTRA_ROOTS="E:\video_demo"` (или включить
   `ALLOW_OUTSIDE=1`).
4. Проверить скорость: задать «что такое 2+2?» — ответ < 10s.
5. Прогнать разогрев модели заранее (warm-up), чтобы в видео не ждать.

## Сценарий по таймкодам

### 0:00–0:30 — Старт и контекст
- На экране: терминал `ollama list` → `python -X utf8 agent.py` → браузер с UI.
- Закадровый текст: «Локальный AI-агент-программист: всё офлайн на
  RTX 3060 12GB, модель qwen3:8b, никаких облаков».

### 0:30–1:30 — Простой цикл «создай и проверь»
- В чат: «создай в E:\video_demo файл hello.py с функцией greet(name),
  возвращающей приветствие, и проверь его запуском».
- Показывать: чтение/создание файла, Syntax: OK, запуск python, ответ агента.
- Текст: «Агент читает, пишет, запускает и отчитывается сам».

### 1:30–2:30 — Рефакторинг (AST-тулы)
- В чат: «в hello.py переименуй greet в greet_user везде, где она
  упоминается».
- Показывать: тул rename_symbol (если влезает) или edit с заменой ВСЕХ
  вхождений, diff-превью, verify.
- Текст: «Переименование с заменой всех вхождений — одно действие».

### 2:30–3:30 — Ревью и фикс сабагентами
- В чат: `@reviewer проверь hello.py`
- Показывать: status «subagent: reviewer», каталог-отчёт (CRITICAL/WARNINGS/
  VERDICT), кнопку 📊 аудита сабагентов (как появился spawn/finished).
- Текст: «Ревью выполняет сабагент с отдельным промптом, аудит пишется
  в лог». Затем `@fixer исправь найденные проблемы` → реальная правка.

### 3:30–4:30 — Датанг-приложение оффлайн (навык)
- В чат: «какими скиллами создашь dating-приложение полностью оффлайн?»
- Показывать: агент сам вызывает skill webapp + skill offline-ollama,
  файловое дерево/чат.
- Текст: «Обучен по методологии: SwiftMatch-класс приложения оффлайн —
  Redis→Map, Stripe→mock, Whisper STT».

### 4:30–5:00 — Итоги
- Показать `/api/stats`, бейдж VRAM, тёмную тему, «126/126 тестов» на
  терминале (скриншот вставкой), ссылку на GitHub.
- Текст: «126 автотестов, бенчмарк 7/7 на qwen3:8b, бенч qwen2.5-coder:7b
  5/7 после few-shot подстройки. Open source: github.com/elips0675-web/
  Myopen-ode».

## Репетиция (замерено 2026-08-12, свежий код stage 73)

| Блок сценария | Тайминг | Что показал агент |
|---|---|---|
| 0:30–1:30 create-file | 31.3s | write + «Syntax: OK» + реальный запуск скрипта |
| 1:30–2:30 rename | 19.7s | AST-тул rename_symbol (1 occurrence) + verify |
| 2:30–3:30 @reviewer | 14.2s | status «subagent: reviewer» → «VERDICT: PASS» |
| 3:30–4:30 skills | 15.0s | текст про архитектуру; скиллы вызваны НЕ всегда — |

Если в кадре агент не вызвал skill webapp/offline-ollama по вопросу из
сценария — повторить вопрос с «вызови скиллы webapp и offline-ollama»
(в предыдущих live-прогонах этот вопрос давал вызов скиллов).

## Чек-лист качества

- [ ] Весь звук разборчив, фоновый шум минимален
- [ ] В кадре виден прогресс агента (тулы/спиннер), а не только финальный ответ
- [ ] Ни одной паузы > 30s (при долгой генерации — закадровый текст)
- [ ] 1080p, 30 fps, формат .mp4 (OBS: x264 CRF 20)
- [ ] Никаких секретов/личных данных в кадре
- [ ] В конце — заголовок с версией и датой

## После записи

1. Залить на YouTube (unlisted) или приложить файлом.
2. Добавить ссылку в TODO.md (пункт P0) и RE_EVAL.md.
3. Подать на внешнюю переоценку 9.5/10 с пакетом RE_EVAL.md + видео.
