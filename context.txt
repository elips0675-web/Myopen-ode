PROJECT: AI Coding Agent (My OpenCode v2)
=========================================
Repo: https://github.com/elips0675-web/Myopen-ode
Author: elips0675-web
Date: 2026-07-31

PURPOSE
-------
Local AI coding assistant that replaces cloud-based tools.
Runs entirely offline on RTX 3060, powered by Ollama + DeepSeek/qwen models.
User types a task in browser, model calls tools to read, edit, write, run code.

KEY ACHIEVEMENT
---------------
- Prompt-based tool calling: local models do NOT support native tool_calls in Ollama.
  Engineered a working agent loop: model outputs ```tool blocks, backend parses, executes, feeds back.
- Streaming tool execution: SSE events (tool/status) streamed live to UI while agent works
- 28 tools: read, write, edit, bash, glob, grep, list, web, websearch, diff, commit, undo,
  verify, plan, search, question, skill, patch, task, todo, lsp, testgen, db_query, deps, mcp
  + 3 plugin tools (count_lines, format_code, git_stats)
- Sessions: SQLite storage (.agent_sessions/sessions.db), auto-migration from legacy JSON,
  JSON fallback if DB unavailable; CRUD + export/import
- RAG: hybrid search (BM25 + semantic) with incremental per-file disk cache
- Self-hosted, zero-cost, fully offline alternative to Cursor/Claude Code
- Feature parity ~90% with OpenCode Desktop (per Kimi evaluation 8.8/10)

ARCHITECTURE
------------
Modular: agent.py (FastAPI) + tools.py (25 tools + 3 plugins) + rag.py (hybrid RAG) +
lsp.py (LSP client) + ui.py (inline HTML UI) + mcp_server.py (MCP for IDEs) +
desktop.py (pywebview native window). Zero npm deps, only fastapi+uvicorn+requests.

1. FastAPI server on port 8765, OpenAPI docs at /docs
2. HTML UI served at GET / (inline, no npm, no build tools)
3. POST /api/chat — SSE streaming endpoint with session support
4. Agent loop: up to 12 iterations, 60s timeout, 3 retries, auto-execute fallback
5. File versioning: backups in .agent_backups before every write/edit/save (up to 50 versions)
6. Verify step: py_compile for .py, tsc --noEmit for .ts/.js, json.tool for .json
7. Git integration: diff + auto-commit
8. Context management: auto-summarization every 3 iterations via 1.5b model
9. RAG: hybrid BM25 + cosine similarity, incremental per-file cache (.rag_cache/)
10. Sessions: full CRUD (create, list, load, delete, export/import JSON)
11. Multi-project: projects.json, per-project sessions/RAG/memory
12. LSP: definition, references, hover, symbols, rename, completion (pylsp/ts-ls/gopls/clangd/bash/css/html)
    + token-based fallback (token_completions in lsp.py) when no language server installed
13. MCP server: all 28 tools via Model Context Protocol (VS Code/Cursor/Claude Desktop)
14. Plugins: .agent_plugins/*.py — dynamic tool registration
15. Terminal: POST /api/terminal — SSE streaming shell, kill endpoint
16. Action audit: .agent_audit.log — every tool call logged with timestamp
17. MCP clients: mcp_servers.json + tool "mcp" — external stdio MCP servers (npx etc.)
18. Live tool progress: run_agent_loop(events=cb) → /api/chat SSE tool/status events

TOOLS (28)
----------
read      — file content (absolute/relative paths + http/https URLs)
write     — create file (with backup + verify)
edit      — replace text in file (with backup)
bash      — run shell command (with cwd support)
glob      — find files by pattern
grep      — search file contents
list      — list directory
web       — fetch URL content
websearch — DuckDuckGo search (no API key)
diff      — git diff
commit    — git add -A + git commit
undo      — restore previous backup
verify    — syntax check a file
plan      — multi-step plan, user confirms
search    — RAG hybrid search (BM25 + semantic)
question  — ask user with answer buttons
skill     — load .agent_skills/*.md instruction
patch     — apply unified diff (line-aware hunks, mismatch -> error, no corruption)
task      — subagents: @explore (read-only), @scout (web), @general (full)
todo      — todo list inside session (add/complete/list)
lsp       — definition/references/hover/symbols/rename/completion
testgen   — generate unit tests (Python, JS/TS)
db_query  — SQL queries against SQLite
deps      — dependency analysis (requirements/pyproject/package.json/go.mod/Cargo.toml/Pipfile)
mcp       — external MCP servers: {server:_list} or {server, call, args}
count_lines / format_code / git_stats — example plugin tools

UI FEATURES
-----------
- CodeMirror editor: tabs, syntax highlighting, code folding, Ctrl+S save (PUT /api/file),
  Ctrl+Space LSP autocomplete (POST /api/lsp/completion)
- Built-in terminal: bottom panel, SSE streaming, command history, Ctrl+C / Kill button
- Chat autocomplete: @files, #skills, /commands with clickable dropdown
- Message history: ↑/↓ arrows, Tab path completion
- Dark theme, mobile-responsive sidebar, drag-and-drop upload
- Diff view with +/- highlighting, confirm boxes, question buttons
- Session export/import, slash commands (/test /deploy /review /fix /doc)
- Model management: +Pull (SSE progress) / -Del
- API link to /docs (Swagger UI)

SAFETY
------
- Directory traversal protection (read/write/edit/patch outside WORK_DIR blocked)
- Bash sandbox: blacklist of dangerous commands (normalized whitespace/quotes,
  recursive check of nested interpreters: bash -c / cmd /c / powershell -c)
- Confirmation before write/edit/bash/commit/undo (with UI buttons)
- Retry with exponential backoff (3 attempts)
- Path validation for glob/grep/list
- NO_CONFIRM=1 skips all confirmations

PERFORMANCE
-----------
- eval_count token counting, in-loop summarization every 3 iterations
- Incremental RAG cache: only changed files re-embedded
- Hybrid search: BM25 (Okapi k1=1.5 b=0.75) + cosine similarity (0.4 weight)
- LLM cache with TTL (LLM_CACHE_TTL=60s, max 100 entries)
- Configurable AGENT_TIMEOUT / AGENT_MAX_ITER / AGENT_MEMORY_LIMIT / bash timeout

HOW AGENT LOOP WORKS
--------------------
1. System prompt tells model it has tools and must output ```tool blocks
2. Model responds: text + ```tool {"tool":"read","path":"file.py"}
3. Backend parses JSON, validates schema, executes tool, appends result as user message
4. Model gets result, decides next step (read more, edit, verify, commit)
5. Loop ends when model responds without a tool block
6. Fallback: if user says "yes"/"да" and no tool block found, auto-executes pending tool

MODELS
------
Primary: deepseek-r1:7b or qwen2.5-coder:7b (RTX 3060, ~5.5GB VRAM)
Planner: deepseek-r1:1.5b — installed (PLANNER_MODEL); auto-fallback to MODEL if missing
RAG: nomic-embed-text
Fallback API: OpenAI / Claude / DeepSeek-V4-Flash (1M context) via FLASH_* env vars
deepseek-coder-v2:16b also fits on 12GB at Q4 (ollama pull deepseek-coder-v2) — optional

RUNNING
-------
python agent.py        ->  http://localhost:8765/
python desktop.py      ->  native pywebview window
python test_agent.py   ->  83/83 smoke tests pass
python mcp_server.py   ->  MCP stdio server for IDEs
pip install -r requirements.txt
AI Desktop.bat         ->  desktop app launcher (python desktop.py)

STATUS
------
Working. Self-hosted. Self-documented. Self-pushed to GitHub.
The agent wrote its own code improvements and committed them.

EVALUATIONS
-----------
Kimi: 7.8/10 — production-ready prototype, но зазор между «работает на демо» и доверием к реальному коду
DeepSeek: 9/10 — зрелый, функциональный, достойная локальная альтернатива облачным решениям
All recommendations implemented: CodeMirror editor, chat autocomplete, LSP rename+completion,
testgen, db_query, deps, interactive terminal, action audit, external MCP clients,
streaming tool execution, hybrid RAG, incremental cache, LLM TTL cache, Swagger.
Fixed real bugs caught by tests: (1) RAG never reindexed changed files at runtime
(RAG_DIRTY stayed False); (2) validate_tool rejected empty-schema tools (list/diff/commit/deps)
as unknown — `if not schema` on {} — agent could never call them.
(3) patch tool: _apply_diff ignored @@ line numbers and concatenated + lines; rewritten
with line-aware hunks (bottom-up apply, context matching), returns error on mismatch instead of corruption.
Sessions now in SQLite (sessions.db) with one-time migration from legacy JSON files.
Thread-safety: locks around LLM cache, todo list, RAG index (RLock) — safe under concurrent uvicorn requests.

Remaining (see TODO.md, priorities from evaluations): Kimi P1 — stream=True tool-block parsing,
RAG indexing in background thread, pydantic arg validation, bash whitelist/shlex, symlink-safe paths;
DeepSeek P1 — FAISS/optimize RAG, size-based chunking. Own ideas: native tool calling (needs Ollama),
Docker bash isolation, Tauri desktop, deepseek-coder-v2:16b pull.
ALL P1s ARE DONE (2026-08-05): stream=True (live streaming), RAG background index, pydantic-style
validation, bash whitelist, symlink safety, FAISS/numpy fast search, size-based chunking.
Open: P2 only — split long functions, CodeMirror 6/Monaco, AST multi-file edit, xterm.js,
Docker bash isolation (DONE 2026-08-06 — BASH_DOCKER=1 opt-in, whitelist enforced, local fallback),
native tool calling (needs Ollama support), Tauri desktop,
deepseek-coder-v2:16b pull (12GB VRAM coding), more LSP/refactor polish.
Open (from eval 2, 2026-08-05): few-shot at tool-error moment, dynamic context line in prompt,
xterm.js+WS (priority 2), integration tests with real model, RAG folder segmentation,
CLI mode, crash recovery, cross-platform tests, user docs, update check, JSON Schema format:"json".
EVAL 3 (2026-08-06, DeepSeek): 8.6/10 — priorities: 1) dynamic context in prompt, 2) integration tests
with real qwen2.5-coder:7b (5-10 e2e scenarios), 3) xterm.js+WS, 4) split monoliths, 5) few-shot at
tool-error. Also: strengthen bare-parser ({"tool":"...} anywhere), VALID/INVALID prompt examples,
first-turn must-read rule, retry with temp 0.1, TOOL_STATS in prompt (careful), Docker readonly/mem/user
flags, /health endpoint, session crash recovery, thinking indicator, RAG progress bar, audit in UI,
troubleshooting guide. Prompt regression guard added: test_system_prompt_rules (rules 16-19 + EXAMPLES).
Tests: 58/58.
UPDATE (2026-08-06b) — eval-3 sprint (63/63 tests + 2/2 live):
(14) DYNAMIC CONTEXT: _dynamic_context() injects "You are working in project: X
(iteration N) / Last action: {tool} (result: ok|error)" into every model call
(7B models lose context after 3-4 iterations). Tests: test_dynamic_context(+error_status).
(15) PROMPT: rule 20 "tools ONLY inside ```tool blocks, else IGNORED" + VALID/INVALID
negative examples; rule 21 "first turn: read at least one file before write/edit".
(16) RETRY temp: Ollama retries (attempt>0) now use temperature 0.1 (deterministic
re-try) in stream_ollama + call_ollama.
(17) TOOL-ERROR FEW-SHOT: nudge now includes concrete fix example
(tried edit/old/new -> error -> corrected glob -> read -> edit).
(18) /health endpoint (status/model/planner/workspace/sessions/rag_chunks/uptime).
(19) DOCKER FLAGS: BASH_DOCKER_READONLY (":ro" mount), BASH_DOCKER_MEM,
BASH_DOCKER_SWAP, BASH_DOCKER_USER.
(20) LIVE INTEGRATION TESTS: test_live.py — real qwen2.5-coder:7b scenarios
(create hello.py/greet 10.8s OK; simple question 0.2s OK), skip if Ollama down.
(21) USER_GUIDE.md (install, first run, tasks, env table, troubleshooting,
skills/plugins how-to).
Tests: 63/63 unit + 2/2 live.
UPDATE (2026-08-07) — sprint 2 (69/69 tests, live CLI OK):
(22) PER-SESSION TOOL ERRORS: _sess_record() counts calls/errors per tool per
session; _dynamic_context appends "Tool errors this session" (max 3 tools) +
"Advice: use glob or list to find the real path before read/edit/write".
Test: test_sess_stats_advice.
(23) MODEL ROUTER: if the active model produces no tool blocks for 2 iterations
(it>0), route to the default MODEL (user_model=None; active_model persists
between iterations; planner is never cached beyond it==0). Fixed the
recompute-each-iteration dead code. Test: test_model_router.
(24) RAG STATUS: RAG_STATUS {phase, files_done, files_total, chunks, updated}
updated in rag_index(); GET /api/rag/status; UI polls every 5s and shows
indexing progress or chunk count. (rag.py needed "import time" - added).
Test: test_rag_status_api.
(25) AUDIT IN UI: GET /api/audit?limit=N (lenient int parse, max 500);
topbar button opens the .agent_audit.log in a popup. Test: test_audit_api.
(26) CLI MODE: python -m myopencode "<task>" (myopencode.py, NO_CONFIRM=1,
prints final output; exit 2 on no args). Live: "what is 2+2?" -> "4".
Test: test_cli_main.
(27) CRASH RECOVERY: .agent_sessions/<sid>.state created on loop start,
touched on checkpoints (save every 2nd iteration), removed on clean finish;
session_interrupted() = stale state (>90s) -> "interrupted": true in
/api/sessions (+"/{sid}"), "⚠ interrupted" badge in UI, resume marker in
history. Test: test_session_checkpoint.
REFRACTOR (2026-08-07) - monolith split (69/69 tests + live CLI OK):
(28) core/agent_loop.py: run_agent_loop + _dynamic_context + summarize_context;
     deps-module injection (config/sessions/pending/cancel via agent.py);
     deps=None -> lazy import agent (CLI/tests unchanged).
(29) core/tool_parser.py: TOOL_BLOCK_PAT/BARE/YAML + _parse_tool_json +
     _strip_system_markers + extract_pending_tool + parse_tool_blocks().
(30) core/tool_executor.py: execute_tool_block(idx, tc, ctx) - anti-repeat,
     aliases, validate_tool, question/plan/CONFIRM branches, _sess_record.
(31) core/safety/bash_guard.py: check_bash(cmd, work_dir) + docker_bash()
     + BASH_BLACKLIST/ALLOWED/NO_DOTDOT; core/safety/path_guard.py:
     resolve/ensure_safe_path/similar_files (work_dir param).
(32) agent.py now HTTP+sessions+pending/cancel+projects (~790 lines), tools.py
     re-exports wrappers (check_bash/resolve/ensure_safe_path bind WORK_DIR).
     Key detail: loop calls deps.call_ollama/stream_ollama/execute_tool so
     tests can still monkeypatch agent_mod.call_ollama.
KIMI EVAL 3 (2026-08-06): 8.7/10 — "most mature open-source coding agent seen on 7B". For 9/10:
split monoliths (agent.py/tools.py -> core/agent_loop.py, core/tool_parser.py,
core/tool_executor.py, core/safety/bash_guard.py, core/safety/path_guard.py),
dynamic context in prompt (project/last action/open tabs), tool-error few-shot,
USER_GUIDE.md. For 9.5/10: xterm.js+WS, live integration test (hello.py/greet),
RAG folder segmentation, CLI mode. Note: reviewer cited 57 tests; we're at 58/58
(+test_system_prompt_rules).
DONE from eval 2: few-shot examples + rules 17-19 (one tool per reply, [DONE] finish, code only via
write/edit), code detector nudge (max 2), TOOL_STATS + /api/stats (warn at 3+ fails),
lenient _parse_tool_json (single quotes/unquoted keys/trailing comma/garbage truncation),
Cache-Control on /static/app.js. [CONFIRM]-timeout NOT needed: loop breaks after CONFIRM by design.
RAG background index crash fixed (inhomogeneous embeddings filtered in _rebuild_fast_index).
CRITICAL OPEN BUG (2026-07-31, live check): confirm flow broken — user "yes" after [CONFIRM] does NOT
re-execute the tool (extract_pending_tool can't find the block in msgs), so agent can't write files
interactively. Fixed same day: model selection now honored (req.model), planner-iteration retries with
main model (1.5b ignores tool format), yaml-style tool blocks parsed. Tests: 29/29.
Recommended coding model: qwen2.5-coder:7b (deepseek-r1:7b sometimes emits free-form tool blocks).
UPDATE (2026-07-31 evening): confirm flow FIXED — _PENDING_CONFIRM stores deferred tool per session,
"yes" auto-executes it without a model call. Live-verified full loop: write → CONFIRM → yes → file
created → verify. Tests: 31/31. Note: server WORK_DIR now defaults to the repo folder (Path(__file__).parent);
set WORK_DIR env to override.
UPDATE (2026-07-31 late): arg validation hardened (types, ranges, enums for task/todo/lsp/mcp/search),
free-form main-model answers get one strict retry hint (deepseek-r1:7b workaround), symlink escape
already blocked (resolve-then-check). Tests: 34/34. Kimi P1 items done: pydantic-style validation,
symlink safety; open: stream=True, RAG background thread, bash whitelist/shlex.
UPDATE (2026-08-05): agent looped on `question` tool (live "Кто ты?" → 12 identical question calls,
no answer). Fixed: (1) question now ends the iteration right after execution — result appended to msgs,
break, loop waits for user reply (no repeated model calls); (2) anti-loop guard: the exact same tool call
twice in a row is blocked ("[tool: identical call repeated — stop repeating, answer directly]" + break),
now also covers broken blocks without a 'tool' key; (3) planner-retry branch no longer fires when the
user explicitly passed a model; (4) "[Format error — retrying with strict hint]" marker no longer leaks
into the visible output. Live-verified: 1 question + real answer. Tests: 36/36.
UPDATE (2026-08-05 late): performance + reliability pass. Root causes of slow/looping behavior:
(1) num_ctx=32768 in call_ollama → 7B Q4 + 32k KV cache overflowed 12GB VRAM → CPU offload → 7 tok/s.
Fixed: num_ctx=16384 (env OLLAMA_NUM_CTX), num_predict=2048 → ~50-60 tok/s (~8x). "Кто ты?" now answers
in ~5s instead of 60s+. (2) AGENT_TIMEOUT default 60→300s (slow models hit TIMEOUT mid-loop; test added).
(3) Server was started without WORK_DIR, so files/skills/plugins/memory were resolved against the old
empty copy E:\My OpenCode instead of the project folder → "read file.py not found", "[SKILL] ... Available:
none". Start with WORK_DIR=E:\My OpenCode1 (context of this project). (4) System prompt hardened:
NEVER invent tools / give tutorials on tool errors, answer in user's language. Live-verified full coding
loop: write → CONFIRM → yes → file created in correct folder. Tests: 38/38 (test_timeout_env,
test_missing_tool_key_stops_loop added). Fast model for chat: qwen2.5-coder:3b (125 tok/s vs 7b's 50-60).
UPDATE (2026-08-05 evening): security + UX pass. (1) Bash sandbox upgraded from blacklist-only to
whitelist (BASH_ALLOWED commands; project-local scripts allowed if they exist in WORK_DIR) + recursive
inspection of python -c/-m and node -e bodies + ".." path-escape ban for destructive commands
(rm/del/cp/mv/...). (2) Graceful cancellation: per-session cancel flag checked between agent iterations,
POST /api/chat/cancel → loop stops and client gets "[cancelled]" (UI Cancel button now actually stops
the agent, not just the fetch). Tests: 52/52 (test_cancel_flag; test_bash_filter extended).
UPDATE (2026-08-05 final): agent-hardening + performance session (commits 2480a59..c2aea27):
(1) outside-workspace error now hints relative paths + list/glob + "no tutorials";
invented-absurd paths blocked in ensure_safe_path (/path/to, /tmp, /var, /usr, /home, /etc,
C:\Windows, C:\Program); prompt rule 16 "never invent paths".
(2) tool-error nudge: after a tool error + prose reply, a system reminder forces the model
back to a corrected ```tool block (max 2 nudges, err_markers: Error:/not found/invalid/
looks invented/outside workspace/missing).
(3) LIVE STREAMING: stream_ollama() (tools.py) streams model output; run_agent_loop emits
{type:"text"} events; /api/chat forwards them to the UI immediately (first text in ~2.6s
instead of silence until the full 9s+ generation; final full-dump skipped if streamed live).
(4) RAG OPTIMIZED: FAISS IndexFlatIP if faiss-cpu present, else numpy matmul, else pure
python; hard memory cap RAG_MAX_CHUNKS=6000 (env); installed numpy 2.5.1 + faiss-cpu 1.15.0.
(5) ERROR HANDLING: json.JSONDecodeError/sqlite3.Error/OSError/ValueError replace bare
excepts in tool-block parsing, session storage, git, confirm scanner; type hints on core
signatures. (6) JS EXTRACTED from ui.py → static/app.js served at GET /static/app.js.
(7) LSP: added clangd (.c/.h/.cpp/.cc/.cxx/.hpp), bash-language-server, css/scss/html
servers + keywords; fixed CREATE_NO_WINDOW typo (cmd window popup on server start).
Tests: 52/52. All Kimi P1 + DeepSeek P1 are DONE.
UPDATE (2026-08-06): stage-1..3 hardening (commit 33833c3 + current):
(8) FEW-SHOT PROMPT: rules 17-19 (one tool per reply; finish with plain text or
[ DONE ] (no ```tool); NEVER write code in text — only via write/edit tools) +
EXAMPLES section (read→answer, edit workflow, bad-path fix). Code detector: prose
containing def/class/import/etc. without a tool → system nudge "use write tool"
(max 2). [CONFIRM]-timeout NOT needed: loop breaks after CONFIRM by design.
(9) RAG CRASH FIX: _rebuild_fast_index skipped malformed/uneven embeddings
(inhomogeneous shape array crash in background indexing).
(10) DOCKER BASH SANDBOX: BASH_DOCKER=1 → docker run --rm -i -v WORK_DIR:/workspace
-w /workspace <BASH_DOCKER_IMAGE default python:3.12-slim> sh -lc "<cmd>"; check_bash
whitelist still enforced; falls back to local shell if docker unavailable.
(11) TOOL STATS: TOOL_STATS per-tool calls/errors in tools.py; GET /api/stats;
warning log at 3+ consecutive errors of one tool.
(12) LENIENT TOOL-JSON: _parse_tool_json() — single quotes, unquoted keys, trailing
comma before }, garbage after block (truncate at last }).
(13) Cache-Control: public, max-age=604800 on /static/app.js.
Tests: 57/57 (test_bash_docker_mode/fallback, test_parse_tool_json_lenient,
test_tool_stats added; docker tests use mocks — no Docker needed in CI).

UPDATE (2026-08-07): этап 8 — xterm.js + WebSocket PTY (commit после 2b94aa7):
(29) REFRACTOR (этап 7): монолиты разбиты на core/ — core/agent_loop.py
(run_agent_loop с deps-инъекцией; deps=None → import agent; цикл зовёт
deps.call_ollama/stream_ollama/execute_tool — тестовые моки работают),
core/tool_parser.py (tool/bare/yaml-парсеры + lenient JSON + strip markers +
extract_pending_tool), core/tool_executor.py (dispatch + anti-repeat + алиасы +
validate + question/plan/confirm + _sess_record), core/safety/bash_guard.py
(whitelist + docker_bash), core/safety/path_guard.py (resolve/ensure_safe_path/
similar_files, work_dir-параметр). agent.py ~790 строк (HTTP+сессии+pending),
tools.py — обёртки с WORK_DIR. 69/69 тестов + live CLI без изменений.
(30) PUSH: 2b94aa7 "refactor: split monoliths into core/ ..." запушен.
(31) PTY (этап 8): core/pty_shell.py — интерактивный шелл: POSIX настоящий PTY
(pty.fork + TIOCSWINSZ), Windows pipes-fallback (env PYTHONUNBUFFERED/
PYTHONIOENCODING). API: feed/read_available/resize/kill, буфер-поллинг ~50ms.
(32) WS TERMINAL: app.websocket /ws/term в agent.py — JSON-сообщения
{cmd|cwd|cols|rows} старт, {input} ввод, {resize} размеры, {kill}; серверные
{out} чанки + {exit} код; auto-kill при disconnect; reader-task фан-аут.
ВАЖНО: uvicorn без websockets/wsproto НЕ умеет WS — websockets>=12.0 добавлен
в requirements.txt (установлен 17.0.1). /static/vendor/{fname} — whitelist
(xterm.min.js, xterm.css) + Cache-Control 3600.
(33) UI: панель терминала переведена с SSE-pre на xterm.js 5.3.0 (vendored в
static/vendor/, CDN не нужен): termStart/termShell/termShellKill/termClear/
termClose; термин-бар: input команды (Enter → запуск шелла с ней, история
стрелками), кнопки New shell/Kill/Clear; resize → TIOCSWINSZ; Ctrl+C → kill.
SSE /api/terminal (+kill) оставлены для совместимости (test_terminal_api).
(34) TESTS: test_pty_shell (python -u -i: ввод → вывод → exit), test_ws_terminal
(TestClient websocket_connect: python -i через WS). 71/71. Live: WS → python -i
интерактив → «>>> 42»; banner приходит с задержкой ~1-2s (python стартует).

COMPARISON
----------
| Aspect        | My OpenCode v2  | Cursor/Windsurf | Claude Code |
|---------------|-----------------|-----------------|-------------|
| On-premise    | YES             | No (cloud)      | No (cloud)  |
| Cost          | $0              | $20/mo          | $20/mo      |
| Model         | 7-16B local     | GPT-4/Claude    | Claude 3.5  |
| Tool calling  | Prompt-based    | Native          | Native      |
| Speed         | Fast (local)    | Network-dep     | Network-dep |
| Sessions      | YES (JSON)      | YES             | YES         |
| RAG           | YES (hybrid)    | NO              | NO          |
| LSP           | YES             | YES             | NO          |
| Editor        | CodeMirror      | VS Code-based   | CLI         |
| Plugins       | YES (py)        | YES             | NO          |

ARCHITECTURE GUIDE (from analysis "how to build an Ollama coding agent")
----------------------------------------------------------------------
CORE FACT: local Ollama models have NO native tool_calls — the model must be
forced to emit ```tool JSON blocks via prompt engineering; backend parses + executes.

3 LAYERS: HTML/CLI UI --HTTP/SSE--> FastAPI agent loop (LLM -> parse -> tool -> LLM)
          --HTTP--> Ollama 127.0.0.1:11434.

AGENT LOOP (run_agent_loop): ask model -> strip <think> (re.sub r'<think>.*?</think>',
done in call_ollama, tools.py:423) -> parse ```tool blocks -> if none: return text ->
execute tools, append "[tool:name] result" as user msg -> repeat until max_iter=12.

PROBLEM -> FIX TABLE (all implemented in this project):
| Model writes prose instead of ```tool       | strict SYSTEM_PROMPT rules 1-3, format_retried strict retry |
| Model invents paths (/path/to/x)            | ensure_safe_path + rule 16 "never invent paths" + invented-absurd path block |
| Model writes "action plan" after tool error | tool-error nudge: system reminder "corrected ```tool block" (err_hint_retried<2) |
| <think> tags pollute output (deepseek-r1)   | re.sub <think> in call_ollama (tools.py:423) |
| Model loops on same tool                    | anti-repeat last_call_key + err_repeat after not-found (repeats>=2 -> stop) |
| Context overflow                            | total_tokens guard MAX_TOKENS, LLM cache TTL, summarization via planner model |
| Dangerous bash                              | BASH_BLACKLIST + confirm before destructive (write/edit/bash/commit/undo) |
| Path escape                                | ensure_safe_path workspace jail + RELATIVE paths only |
| No live progress in UI                      | SSE events from run_agent_loop(events=cb) -> /api/chat stream |

MODELS (installed via ollama): deepseek-r1:7b (~5.5GB main-capable but <think>-
heavy, ignores tool format sometimes), qwen2.5-coder:7b (~5GB, best code +
fewer hallucinations, now DEFAULT via AI_MODEL), deepseek-r1:1.5b (~1.5GB planner/
summary), nomic-embed-text (~0.5GB RAG embeddings), qwen2.5-coder:1.5b/3b,
llama3.1, qwen3:8b.

STAGE 9 (done 2026-08-07): CLI mode (python -m myopencode "task", NO_CONFIRM=1);
session checkpoint state-json each 2 iters + "interrupted" marker + resume note;
model router: it>=2 && no tool blocks -> switch to main model (planner = router);
RAG folder scope: rag_search(scope="core/") linear pass over cached chunks, tool
search passes args["scope"]; JSON Schema: TOOL_JSON_SCHEMA + set_json_mode
(thread-local) + AI_JSON_FORMAT=1, loop enables after nudges, disables each iter;
cross-platform CI matrix .github/workflows/tests.yml; /api/update (git HEAD vs
origin, 1h cache, offline-safe) + "updst" badge; hierarchical subagent: task
tool runs sub-loop (run_agent_loop, NO_CONFIRM=True, fallback single call);
git pre-backup: auto snapshot before 1st mutating tool (git diff --binary + copy
untracked, skip .git/.agent_backups), tools snapshot/restore (restore = checkout
-- . + re-apply patch + sync untracked); inline diff: execute_tool emits 'diff'
event, UI renders .dp/.a/.d/.h; CodeMirror 5.65 vendored static/vendor/cm/ via
StaticFiles mount (offline UI); MCP client full handshake (initialize ->
notifications/initialized -> capabilities, resources/list|read,
prompts/list|get, tools/list/call). 80/80 tests. Commit b5994ea (not pushed).

STAGE 10 (done 2026-08-07): Native tool calling via Ollama /api/chat "tools".
tools.py: native_chat(messages, model, tools) -> (content, tool_calls, tokens),
native_supported(model) — autodetect by name prefix (AI_NATIVE_MODELS default
"qwen3,llama3.1,gpt-oss", disable AI_NATIVE_TOOLS=0), native_tools_schema()
(TOOL_SCHEMAS -> Ollama function format), NATIVE_SYSTEM_PROMPT (legacy rules
about ```tool blocks REMOVED — the full SYSTEM_PROMPT makes qwen3 return text
instead of tool_calls, verified by probe). agent_loop native branch: swaps
system prompt, calls native_chat, executes tool_calls via execute_tool_block
(multiple calls per turn), empty calls -> final answer, failure -> legacy
fallback. qwen2.5-coder:7b (default model) does NOT support native (returns
JSON in content) — legacy path untouched for it. Live-verified with qwen3:8b
(write -> confirm -> verify). 82/82 tests. Commits 3bc58f4 (stage 9), 619c3ea
(docs/tool sync).

STAGE 11 (done 2026-08-07): Desktop App pywebview. desktop.py rewritten:
is_port_open() reuses an already-running server (no double uvicorn bind),
wait_server_ready() polls GET /health before opening the window (no more
blocking wait_ollama — server survives missing Ollama), 1280x860 window with
min_size, title "My OpenCode", icon from assets/icon.ico; fallback to browser
when pywebview fails; --browser flag. scripts/make_icon.py generates
assets/icon.png+icon.ico (hand-rolled PNG/ICO via zlib, no PIL). Tauri NOT
used — Rust/MSVC toolchain not installed. 83/83 tests.

REMAINING (as of 2026-08-07): deepseek-coder-v2:16b pull interrupted at 26%
(8.9GB total, ~5.4MB/s, "error: unexpected EOF") — resume with
`ollama pull deepseek-coder-v2:16b`; then probe native tool-calling support
(not in AI_NATIVE_MODELS default) and live coding test on RTX 3060 12GB.
GPU embeddings: explicitly NOT needed — Ollama already on GPU (qwen3:8b 7.5GB
100% GPU).
RESOLVED (2026-08-07): pull resumed and completed (8.9GB). deepseek-coder-v2:16b
does NOT support native tool calling — Ollama returns HTTP 400 when "tools" is
sent (like qwen2.5-coder; only qwen3/llama3.1/gpt-oss accept tools). It works
via the legacy ```tool pipeline. Live coding cycle verified: write ->
[CONFIRM] -> yes -> file created (8 bytes, exactly one audit entry). NOTE:
audit log only records tools that actually execute — CONFIRM-armed writes
appear only after "yes". All TODO items now resolved or explicitly skipped.

UPDATE (2026-08-08) — stage 13: multi-model live suite + router fix (83/83):
(1) test_live.py now runs create/edit/question scenarios across ALL installed
models (default qwen2.5-coder:7b, qwen3:8b, deepseek-coder-v2:16b), per-model
warm-up, flags --models/--full, honest per-model report. Honest results
(stochastic between runs due to 12GB VRAM eviction): qwen3:8b (native) ~3/3
best; qwen2.5-coder:7b ~2/3 (edit-rename answers with instructions); 16b
~0-1/3 (broken ```tool blocks, lists instead of answers).
(2) ROUTER FIX (design, asked user): user-selected model is NEVER silently
routed to default — live test showed user picked 16b but qwen2.5-coder:7b
was actually executing. Dead router branch removed, planner-retry (it==0)
kept. test_model_router rewritten (never routed), test_planner_router dropped.
(3) Empty model response retried once before break (retried_empty flag) —
"No response from model" on 16b under VRAM pressure.
(4) Docs synced: README (test_live.py, 83/83), TODO (stage 13), USER_GUIDE
(CLI, desktop, xterm/WS, native tools, env vars), context, KIMI_REVIEW_SUMMARY
(stages 8-13, P2 list reduced to: monolith refactor, TOOL_STATS in prompt,
UI dynamics, CodeMirror 6/Monaco, AST multi-file edit, Tauri).

UPDATE (2026-08-08) — stage 14: tools.py monolith split into tools/ package (83/83):
(1) tools/_state.py = all mutable globals + init_config with _sync_register():
    every submodule keeps a copy of the config names and registers itself,
    init_config setattr-s both the state module and all copies. BACKUP_DIR/
    AUDIT_LOG/PLUGINS assignments inside submodules go through their local
    globals (same objects as state for dicts; re-assignments like PLUGINS={}
    in load_plugins are mirrored to _state).
(2) tools/paths.py, backup.py (backup/undo/git/verify/prebackup/restore/diff),
    plugins.py, llm.py (call/stream/native/fallback + TOOL_DESCS), audit.py,
    exec.py (validate_tool/execute_tool/_execute_tool_inner/check_bash/diff
    parser), __init__.py = facade re-exporting EVERYTHING public + private
    helpers used by tests + module attrs tests monkey-patch (tools.requests).
(3) exec.py reads BASH_TIMEOUT via _state module (live value); task tool
    accesses SUBAGENT_PROMPTS lazily via `import tools` (circular import safe);
    websearch/read/web use lazy imports (equivalent behavior).
(4) BREAKING-NOTE for tests: tools.WORK_DIR = x no longer propagates to
    submodules — use tools.init_config(WORK_DIR=x) (test_git_snapshot_restore
    updated). Other modules (agent.py, mcp_server.py, agent_loop) use
    init_config or read-only attr access — unaffected.
(5) Verified: 83/83 tests, CLI live ("4"), server restarted on new layout
    (/health 200).

UPDATE (2026-08-08) — stage 15: _execute_tool_inner split (83/83):
(1) The 366-line _execute_tool_inner became a dispatcher (_TOOL_DISPATCH)
    over 26 per-tool handlers _tool_* in tools/exec.py; plugin fallback kept.
    Byte-identical branch behavior — verified by the full 83-test suite and
    a live CLI run ("4").
(2) Remaining P2: agent.py (~880 lines FastAPI) route split, TOOL_STATS in
    system prompt (per-session stats already covered by sess_stats in
    _dynamic_context), CodeMirror 6/Monaco, AST multi-file edit, Tauri
    (blocked on Rust/MSVC).





UPDATE (2026-08-08) - stage 16: agent.py route split (83/83):
(1) agent.py (883 lines) reduced to ~470: app + index/static/chat(+cancel) routes,
    sessions storage (SQLite+JSON), memory, projects, pending/cancel state.
    New modules: api_sessions.py (CRUD/search/export/import), api_files.py
    (browser/editor/upload), api_misc.py (stats/health/update/models/projects/
    task/plugins/skills/terminal/ws/lsp). Registered via app.include_router at
    the end of agent.py; orphan imports (subprocess/WebSocket/glob) cleaned.
(2) IMPORTANT special case: running python agent.py executes the module as
    __main__, so import agent inside api_* would reload a FRESH copy of
    agent.py -> circular ImportError at the include_router line. Fix: at the
    top of agent.py, when __name__=="__main__" and "agent" not in sys.modules,
    register sys.modules["agent"] = sys.modules["__main__"]. Verified with a
    real server start (bind error 10048 was the old process; second attempt
    healthy).
(3) Mutating globals (WORK_DIR/SESSIONS_DIR change on switch_project): api_*
    modules read them dynamically via import agent as _agent (from-import
    would capture a stale copy). Functions (_db, load_session, ...) resolve
    their globals inside agent.py at call time - unaffected.
(4) Tests updated: test_health_endpoint/test_update_check now call
    api_misc.health()/update_check(), test_session_search -> api_sessions.
    83/83, CLI live ("4"), server restarted (/health 200, session CRUD probe
    via HTTP ok).

UPDATE (2026-08-08) - stage 17: TOOL_STATS in system prompt (84/84):
(1) _dynamic_context now appends "Global tool stats (all sessions):" - top-3
    tools with repeated failures (calls >= 2, sorted by error count) + the
    same glob/list advice. Single-failure tools are excluded (noise).
(2) Stats resolved lazily via import tools inside _dynamic_context - CLI
    and tests don't pass anything; optional tool_stats= kwarg keeps the
    unit test network-free. Per-session "Tool errors this session" kept.
(3) New test: test_dynamic_context_global_stats (asserts top-3 + exclusion
    of single-failure tools). 84/84, CLI live ("4"). P2 left: CodeMirror 6/
    Monaco, AST multi-file edit, Tauri (blocked on Rust/MSVC).

UPDATE (2026-08-08) - stage 18: AST syntax guard + multi-file patch (86/86):
(1) _syntax_check(path) in tools/exec.py after write/edit/patch: .py ->
    ast.parse (stdlib, reports line number), .json -> json.loads, .js/.mjs/
    .cjs/.ts -> node --check when node exists; other extensions -> None
    (silently skipped). Reply gains "Syntax: OK" / "Syntax: ERROR: msg (line N)".
    Model can self-correct broken code without a manual compile step.
(2) patch tool: new mode files=[{path, diff}, ...] - multiple files in one
    call, applied in order; each file: ensure_safe_path, backup, _apply_diff
    (None -> per-file "mismatch", other files still proceed), verify_file,
    _syntax_check. Legacy path+diff mode untouched.
(3) validate_tool: when patch has a files list, required path/diff are not
    enforced; each diff validated via _validate_patch (hunk headers), shape
    check ({path, diff} strings) - errors reference the file. NOTE: validate
    runs in the agent loop (core/tool_executor.py) and mcp_server, NOT in
    bare execute_tool - tests call validate_tool directly for that path.
(4) _apply_diff semantics kept: '-' lines just skip a line (no content
    match), only ' ' context lines are matched - mismatch tests use a
    context line. 86/86, CLI live, server restarted.

UPDATE (2026-08-08) - stage 19: CodeMirror 6 replaces CodeMirror 5 (86/86):
(1) Built an offline CM6 bundle via esbuild (IIFE, 601KB) in a temp dir
    (npm i codemirror @codemirror/lang-{python,javascript,json,html}
    @codemirror/theme-one-dark; entry.js -> window.cm6 exports; npx esbuild
    --bundle --minify --format=iife). Output: static/vendor/cm6.bundle.js.
    NOTE: newer codemirror@6 no longer re-exports EditorState/keymap from
    the top-level package - import from @codemirror/state and @codemirror/view.
(2) ui.py: the 15 CM5 <script> tags replaced by one cm6.bundle.js tag;
    inline flag: var CM_READY = typeof cm6 !== 'undefined'. The static
    /vendor/{fname} route only matches ONE path segment - the bundle lives
    flat in static/vendor/ (not static/vendor/cm6/), whitelist extended.
(3) app.js: makeEditor returns EditorView (basicSetup + oneDark +
    foldGutter() + keymap Ctrl-s/Cmd-s -> saveFile + autocompletion
    override cmCompleteSource -> POST /api/lsp/completion with
    line=lineAt(pos).number-1 (CM6 positions are 0-based vs CM5 0-based too
    but lineAt gives 1-based line); renderEditor destroys previous view
    (view.destroy) and sets content via dispatch changes; saveFile uses
    state.doc.toString(). Dead CM5 popup function editorComplete removed;
    closeTab destroys the view instead of getWrapperElement().remove().
(4) RAG flakiness fixed: 616KB minified bundles got indexed (vendor/ wasn't
    in SKIP_PARTS) and polluted search ranking - added "static","vendor" to
    SKIP_PARTS. test_rag_search_via_execute stable across two full runs.
(5) 86/86 x2, CLI live ("4"), server restarted (index serves cm6 script,
    /static/vendor/cm6.bundle.js 200, /health 200). P2 left: Tauri only.

UPDATE (2026-08-08) - stage 20: Tauri desktop done (P2 fully closed, 86/86):
(1) Toolchain: winget Rustlang.Rustup (rustc 1.97.1, msvc target default) +
    winget Microsoft.VisualStudio.2022.BuildTools --override "--quiet --wait
    --norestart --add Microsoft.VisualStudio.Workload.VCTools --add
    Microsoft.VisualStudio.Component.VC.Tools.x86.x64 --includeRecommended"
    -> cl.exe at Program Files (x86)...BuildTools. Add C:\Users\admin\.cargo\bin
    to PATH in every shell (rustup installs there, not auto-added to PATH of
    existing processes).
(2) src-tauri/ = cargo-only wrapper (NO npm needed): Cargo.toml (tauri 2,
    tauri-build 2, edition 2021, release opt-level s), build.rs, tauri.conf.json
    (productName "My Open Code", identifier com.myopencode.desktop, devUrl
    http://127.0.0.1:8765, frontendDist ../dist as dummy because bundle.active
    = false, window 1280x860 center min 800x600, bundle inactive, icon
    icons/icon.ico), src/main.rs: if :8765 closed -> spawn python -X utf8
    agent.py in project cwd (MYOPENCODE_PYTHON/MYOPENCODE_PORT env), poll
    TcpStream connect 0.5s x 240; tauri::Builder::default().run. Icons copied
    from assets/icon.ico.
(3) Build: cargo build in src-tauri/ (first ~2m16s) -> target/debug/myopencode.exe
    (target/ is gitignored by src-tauri/.gitignore). Launch verified: process
    alive + 3 msedgewebview2 children (WebView2 window really opened).
    scripts/run_tauri.bat: builds on first run, prints toolchain install
    instructions if cargo missing, starts the exe.
(4) devUrl-only approach: release/bundle build does NOT make sense here -
    the whole UI is served by agent.py (ui.py HTML string + /static), so the
    wrapper always targets the live server (same reuse logic as desktop.py).
(5) Docs: KIMI stage 34 + "Что осталось (P2)" closed, TODO stage 20, README
    desktop mentions (Tauri/WebView2 + pywebview), USER_GUIDE new section
    with both launchers + winget toolchain commands. 86/86, CLI live ("21"),
    /health 200. Test instance of the exe was closed after verification.

UPDATE (2026-08-08) - reviews 8.8 (external) + 8.9 (DeepSeek 4) received; plan to 9.5/10:
Score line: Kimi 8.3 -> DS 8.5 -> DS 8.6 -> Kimi 8.7 -> ext 8.8 -> DS4 8.9.
Review files: KIMI_REVIEW_SUMMARY.md (full list of stages 1-20 + review 4 + plan),
Оценка kimi.txt (7.8, 8.7, 8.8 sections), Оценка deepseek.txt (8.5, 8.6, 8.9 sections).

TODO to reach 9.5/10 (prioritized, NOT started yet - codebase clean at HEAD dcae868):
P1 (production-critical, both reviewers agree):
(1) AST-based edit guard - in tools/exec.py _tool_edit/_apply_diff: check uniqueness of
    old text (count occurrences); if N>1 or fuzzy match <90% -> warning to model
    "old text found N times, make it unique" BEFORE applying. Test: edit with duplicate.
(2) Git-auto-branch - per session: create branch agent-session-<id>, auto-commit after
    each write/edit/patch, undo = git reset (keep .agent_backups/ as fallback). Careful:
    WORK_DIR may not be a git repo (path_jail still covers); CI matrix + tests.
(3) Prompt KV-cache - after iteration 3, swap full SYSTEM_PROMPT rules for compressed
    summary (rules 1-21 -> 5-6 lines); system stays first message for Ollama KV cache.
    Keep test_system_prompt_rules passing (it checks the full prompt - gate by iteration).
(4) AUTO_CONFIRM_SAFE=1 (DS4) - auto-approve write/edit for NEW files only (no overwrite
    of existing, no bash); NO_CONFIRM stays all-or-nothing.
(5) Default model qwen3:8b when VRAM >10GB - probe via ollama ps (0 GPU/CPU fields) or
    nvidia-smi; fallback qwen2.5-coder:7b. Env override AI_MODEL stays.
(6) Docker sandbox default when docker present + log warning (DS4) - auto BASH_DOCKER=1
    if docker info succeeds, with warning line; fallback local shell.
P2 (distinction from mature prototype):
(7) Task-level model router - zero-shot classify task (bugfix/refactor/chat/tests) with
    PLANNER_MODEL BEFORE first call; map to model (bugfix->qwen3, refactor->16b, chat->3b).
    Do NOT override user-picked model (stage 13 lesson!). Only propose/advise.
(8) Plan tree UI - /api/plan for plan tool output; render checklist pending/done/error
    (like Cursor Composer); poll or SSE events.
(9) ARCHITECTURE.md with Mermaid diagram: UI -> FastAPI(agent.py/api_*) -> agent_loop
    (core/) -> tools/ dispatch -> Ollama/native + per-tool examples (DS4).
(10) AST refactor tools (DS4): rename_symbol / extract_function / inline_variable via
     stdlib ast + tokenize (python only; no libcst dep). 3 new _tool_* handlers + schemas.
(11) VRAM indicator in UI + auto model pick at startup (DS4).
(12) Docker flag: BASH_DOCKER default... (same as 6).
P3 (10/10):
(13) Self-healing loop - after 2 errors on same tool: switch strategy (edit -> read+write)
     automatically (TOOL_STATS already drives advice text).
(14) Multi-turn RAG ("RAG over plan") - first pass: find all affected files via search;
     second pass: edit them. Plan-first tool call chain.
(15) Voice input (Whisper+STT) - long-term, DS4 9.8-10 item.
Other notes: tool stats UI chart (DS4 2.4), prompt ceiling warning (ext review), unquoted
string values in _parse_tool_json, semantic arg validation ("path looks like a directory"),
STRUCTURED prompt (XML tags) alternative - all lower priority than P1 list.
After implementing P1(1-3): rerun 86/86 + CLI live + server restart, then KIMI stage 21+
entries, TODO checkboxes, commit+push per stage. Update README when stable.

UPDATE (2026-08-08) - stage 21 done (P1 #1 AST-based edit guard), HEAD pending push:
- tools/exec.py: _edit_old_stats(content, old) - returns (count, fuzzy) where fuzzy =
  closest line via difflib.SequenceMatcher (ratio >= 0.8). _tool_edit now: count>1 ->
  reject WITHOUT backup/mutation, message "found N times - ambiguous, make old unique
  (include surrounding lines)"; count==0 -> "text not found" + "Closest match: ..."
  hint. Edit never mutates on ambiguity/typo. 88/88 (2 new tests:
  test_edit_guard_ambiguous, test_edit_guard_fuzzy_hint), CLI live, server restarted.
Next stages: (2) Git-auto-branch, (3) Prompt KV-cache, (4) AUTO_CONFIRM_SAFE,
(5) qwen3:8b default >10GB VRAM, (6) Docker default.

UPDATE (2026-08-08) - stage 22 done (P1 #2 Git auto-branch/commit), 89/89 x3:
- tools/_state.py: GIT_AUTO_COMMIT / GIT_AUTO_BRANCH (env, default 0).
- tools/backup.py git_auto_commit(rel, tool): opt-in; wd = _s.WORK_DIR.resolve();
  paths normalized "\\"->"/" and relative-to-wd (abs outside repo -> None);
  GIT_AUTO_BRANCH creates agent-session-<ts> branch on FIRST commit;
  git add -- <rel> + git commit --no-verify -m "agent: <tool> <path>";
  returns "git: <hash> committed" or None (no repo/disabled/fail). Appended to
  write/edit/patch results.
- IMPORTANT fix: rel path in _tool_write/_tool_edit was computed against the
  STALE exec.py copy of WORK_DIR (frozen at import) -> when tests changed
  _state.WORK_DIR the rel was wrong and git add failed on Windows backslashes.
  Now rel uses _s.WORK_DIR.resolve(). Also git add needs forward slashes.
- Test test_git_auto_commit: temp repo via git init -b main (no checkout!),
  force rmtree with chmod onerror (Windows .git read-only files block plain
  rmtree - that was the "temp repo cleanup failed" flake), write+edit ->
  "git:" in result, branch starts agent-session-, main log still "base".
Next: (3) Prompt KV-cache, (4) AUTO_CONFIRM_SAFE, (5) qwen3 default >10GB,
(6) Docker default when present.

UPDATE (2026-08-08) - stage 23 done (P1 #3 Prompt KV-cache), 90/90 x2:
- tools/__init__.py: COMPACT_SYSTEM_PROMPT (contains "RULES" + "[COMPACT SYSTEM
  PROMPT]" markers, ~0.3K tokens vs ~2K full block) + compact_system_prompt().
- core/agent_loop.py: on it >= 3 swap msgs[0] (system RULES message) to compact
  once (compacted flag; "COMPACT" guard prevents double swap; native branch still
  matches "RULES" and replaces with native_system_prompt()).
- NOTE: compact prompt must KEEP "RULES" in first 4000 chars - native-call
  branch detects the system prompt by that marker (core/agent_loop.py:200).
- Test test_compact_prompt_after_iterations: 5 unique tool calls (list ./tools,
  glob, grep) to avoid anti-loop identical-call blocking; events path uses
  stream_ollama (mock BOTH call_ollama and stream_ollama!). 90/90 x2, live 4/9
  (usual variance), CLI live, server restarted.
Next: (4) AUTO_CONFIRM_SAFE, (5) qwen3:8b default >10GB VRAM, (6) Docker default.

UPDATE (2026-08-08) - stage 24 done (P1 #4 AUTO_CONFIRM_SAFE), 91/91 x2:
- core/tool_executor.py: _auto_confirm_safe(name, tc) - AUTO_CONFIRM_SAFE=1 +
  write + target file does NOT exist -> execute immediately (no [CONFIRM]);
  all other destructive ops unchanged. NOTE: 'last' var must be computed
  before the _auto_confirm_safe branch (yes-autoexec relies on it).
- Test test_auto_confirm_safe: env set/restored in finally; new file auto-
  written, overwrite of existing file still [CONFIRM] and file untouched.
- Live CLI with AUTO_CONFIRM_SAFE=1: write called without CONFIRM (visible as
  "(called: write)"); the 7B model kept emitting broken blocks that run - that
  is the known model-behavior limit, not the feature.
Next: (5) qwen3:8b default >10GB VRAM via ollama ps, (6) Docker default when
present. Then P2: (7) task-level router, (8) plan tree UI, (9) ARCHITECTURE.md,
(10) AST refactor tools, (11) VRAM indicator.

UPDATE (2026-08-08) - stage 25 done (P1 #5 auto model pick), 92/92 x2:
- agent.py _auto_pick_model(): no explicit AI_MODEL + ollama list has qwen3:8b
  + nvidia-smi total >=10GB -> default MODEL = qwen3:8b (log 'Auto-picked').
  Called at import. import subprocess moved to top (tests mock
  agent_mod.subprocess.run).
- BUG FIX 1 (stage 25 side effect): auto-pick switched the test model to
  native-capable qwen3:8b -> all loop tests that mock ONLY legacy
  call_ollama broke ('model never called', real model answers). Fix: test
  runner main forces MODEL to qwen2.5-coder:7b when native_supported(MODEL).
- BUG FIX 2 (RAG): 3 corrupt .rag_cache files (embeddings dim 2 / dim 0,
  leftovers) + _rebuild_fast_index used len(RAG_INDEX[0]) -> one bad entry
  killed the whole index ('RAG search error: '). Fix: dim = max over all,
  _load_file_cache filters pairs; corrupt files deleted.
- Live: qwen3:8b answered via tool verify (native path works). Server
  restarted with auto-picked model.
Next: (6) Docker default when present. Then P2: (7) task-level router,
(8) plan tree UI, (9) ARCHITECTURE.md, (10) AST refactor tools,
(11) VRAM indicator.

UPDATE (2026-08-08) - stage 26 done (P1 #6 Docker), 93/93 x2:
- core/safety/bash_guard.py docker_bash: now enabled by BASH_DOCKER=1 OR
  DOCKER_SANDBOX=1 (both work). Safe 'default when present' interpretation:
  auto-detect at startup + log suggestion, NO automatic replacement of the
  local shell (python:3.12-slim lacks git/pip/node - would break verify/commit).
- agent.py _detect_docker(): one 'docker version' call (5s timeout) at
  import; on success logs 'set DOCKER_SANDBOX=1 to enable'; DOCKER_SANDBOX=0
  disables both detection and sandboxing.
- Test test_docker_sandbox_flag: mocks bash_guard.shutil.which + subprocess.run
  (no flag -> None/local; =1 -> docker run invoked; =0 -> local; BASH_DOCKER
  alias still works). Docker is NOT installed on this machine - detection
  silently returns False.
- Server restarted (health 200).
All P1 (DS4) items done. Next: P2 - (7) task-level router, (8) plan tree UI,
(9) ARCHITECTURE.md, (10) AST refactor tools, (11) VRAM indicator.

UPDATE (2026-08-08) - stage 28 done (P2 #10 AST refactor tools), 94/94:
- tools/exec.py: rename_symbol / extract_function / inline_variable,
  registered in _TOOL_DISPATCH and TOOL_SCHEMAS.
- rename_symbol: exact ast.Name/arg/def/class matches, byte-offset edits
  applied from the end, ast.parse guard before write, backup + git_auto_commit.
  NOTE py3.14: FunctionDef/ClassDef col_offset points at 'def'/'class' NOT
  the name (line.find from col_offset); end_col_offset = end of whole block.
- extract_function: explicit params/call_args from the model; dedents body by
  base indent; function appended at EOF.
- inline_variable: removes 'var = expr' line, replaces later ast.Name uses
  with source segment of expr.
- Live CLI: model renamed total->sum_total via rename_symbol (file correct).
- Server restarted (health 200).
Remaining P2: (7) task-level router, (8) plan tree UI, (11) VRAM indicator.

UPDATE (2026-08-08) - stage 29 done (P2 #11 VRAM indicator), 95/95 x2:
- api_misc.py _vram_info(): nvidia-smi memory.total,used, cached 10s;
  GET /api/vram + "vram" field in /health. Graceful (ok:false) without GPU.
- ui.py: #vram-badge in header; static/app.js vramPoll() every 15s ->
  'VRAM 7.3/12.0 GB (61%)', red bg above 90%.
- Test test_vram_indicator (mock nvidia-smi parse + missing GPU).
- Live: /api/vram {total_mb:12288, used_mb:9430, ok:true}.
- Server restarted (health 200).
Remaining P2: (7) task-level router, (8) plan tree UI.

UPDATE (2026-08-08) - stage 30 done (P2 #7 task-level router), 96/96 x2:
- tools/llm.py pick_task_model(task, base): zero-shot classify via PLANNER_MODEL
  (1 call, temperature 0) BEFORE the loop; bugfix/refactor/tests -> qwen3:8b,
  chat -> qwen2.5-coder:3b. Explicit AI_MODEL wins; req.model (user pick) is
  never overridden; tasks <20 chars or base==qwen3:8b skip classification;
  target not installed (cached /api/tags, 60s TTL) -> default kept.
- Wired into /api/chat (chosen_model computed before run_agent_loop) and CLI
  (myopencode.py passes model= to the loop).
- Test test_task_router (6 cases; note qwen2.5-coder:3b NOT installed on this
  machine - chat branch tested with mocked installed list).
- Live: CLI answered '10'. Server restarted (health 200).
Remaining P2: (8) plan tree UI.

UPDATE (2026-08-08) - stage 31 done (P2 #8 plan tree UI, LAST evaluation item), 97/97 x2:
- tools/_state.py: PLAN_STEPS + PLAN_LOCK.
- core/tool_executor.py: plan branch persists steps as pending + emits
  {type:'plan', steps}; _plan_mark(ctx, result) called after every executed
  tool (auto-confirm/yes/generic paths) marks first pending done/error and
  re-emits. ctx.state needs last_result_name/last_call_key (test got it).
- static/app.js planTree(steps): tree with check/cross/circle, styles in
  ui.py (.plantree/.plstep/.pldone/.plerr/.plpend).
- Test test_plan_tree_events: plan -> 2 pending + event; tool -> first done;
  Error tool -> error; new plan resets.
- Live CLI: model produced [PLAN] with 2 steps + 'Reply yes' (plan tool
  works; step wording stochastic).
- Server restarted (health 200).
ALL evaluation items (Kimi 8.3 / DS 8.5 / DS 8.6 / Kimi 8.7 / external 8.8 /
DS4 8.9) are implemented. Next options: new external review, 9.5 push (P3:
self-healing loop, multi-turn RAG, voice input), or polish.

UPDATE (2026-08-08) - external review #2 (Kimi file): 8.9/10 (+0.1), оценка 5:
- Based on stage 20 artifacts (Tauri done, 86/86 tests, ratings 8.7/8.8/8.9).
- Verdict: 'production-ready beta'; Tauri closed the last big P2; remaining
  P1 = edge-case polish, not architectural holes.
- Axis scores: Arch 9.0, Code/tests 9.5, Safety 8.5, AI 8.2, UX 8.3, Docs 9.0.
- Plan to 9.5: P1 (edit guard, git-auto-branch, prompt KV-cache), P2 (task
  router, plan tree UI, ARCHITECTURE.md), P3 (self-healing loop, multi-turn
  RAG).
- STATUS: ALL P1+P2 items were ALREADY closed (stages 21-31): edit guard=21,
  git-auto-branch=22, KV-cache=23, AUTO_CONFIRM_SAFE=24, auto model=25,
  Docker=26, ARCHITECTURE.md=27, AST tools=28, VRAM=29, router=30, plan
  tree=31. Tests now 97/97 x2 (86/86 at review time).
- Files updated: 'Оценка kimi.txt' (ОЦЕНКА 5 section), KIMI_REVIEW_SUMMARY.md
  (header line + section), TODO.md (rating line), README.md (ratings line).
- Remaining to 9.5: P3 - self-healing loop, multi-turn RAG, voice input.

UPDATE (2026-08-08) - DeepSeek re-evaluation (Оценка deepseek.txt): 8.9/10 (evaluation 5):
- Same eval as external #2 (8.9): Stage 20 Tauri desktop (WebView2, ~50MB RAM
  vs ~300MB pywebview), 86/86 tests at review time, repo github.com/elips0675-web/Myopen-ode.
- Verdict: 'production-ready product', project keeps position as most mature
  local open-source Cursor/Claude Code alternative; Tauri closed the last P2.
- P1 list at review time: AST edit guard, git-auto-branch, prompt KV-cache,
  AUTO_CONFIRM_SAFE, qwen3:8b default >10GB, Docker default - ALL now done
  (stages 21-26). P2: task router, plan tree, ARCHITECTURE.md, AST tools,
  VRAM indicator - ALL done (stages 27-31). Tests now 97/97 x2.
- Remaining to 9.5: P3 - self-healing loop, multi-turn RAG, voice input.
- Files updated: 'Оценка deepseek.txt' (ОЦЕНКА 5 section), KIMI_REVIEW_SUMMARY.md
  (DeepSeek bullet), TODO.md rating line, README.md ratings line.

UPDATE (2026-08-08) - docs sync with evaluation plan status (Что доделать):
- TODO.md P2: ARCHITECTURE.md marked [x] (Этап 27, DS4 item); Desktop App line
  updated (Tauri = Этап 20, Rust 1.97.1 + MSVC installed).
- KIMI_REVIEW_SUMMARY.md: 'План до 9.5/10 (по оценке 4)' - P1 (edit guard=21,
  git-auto-branch=22, prompt KV-cache=23) and P2 (router=30, plan tree=31)
  marked [x]; 'Что осталось (P2)' section notes ALL plan items closed 21-31,
  remaining P3 (self-healing loop, multi-turn RAG, voice input); agent.py
  DI note updated (partially closed by stages 7/16, DI container out of 9.5 scope).

UPDATE (2026-08-08) - P3 done (stages 32-34), commit b85800f, tests 99/99 x2:
- Stage 32 self-healing loop: consecutive same-tool errors tracked (err_streak/
  err_tool in run_agent_loop); after 2+ in a row _dynamic_context injects
  'SWITCH STRATEGY NOW' advice (glob/read first, edit with EXACT text or
  rewrite with write). test_self_healing_advice.
- Stage 33 multi-turn RAG ('RAG over plan'): plan branch runs rag_search over
  each step (up to 4), collects top file paths (max 6), reads them (2KB each,
  total cap 10KB) into a system block 'Plan context' appended after the plan
  message; status event emitted; AI_RAG_OVER_PLAN=0 disables; never raises.
  test_rag_over_plan; live CLI: plan built with pre-loaded context.
- Stage 34 voice input: mic button (#mic) in UI next to Send; Web Speech API
  (SpeechRecognition/webkitSpeechRecognition, ru-RU, interimResults, continuous)
  fills the textarea; button hidden when API unsupported; Whisper server-side
  remains future option.
- UI verified: mic button + toggleMic in served HTML/JS; server restarted,
  /health 200 (qwen3:8b, 43 sessions).
- Docs: TODO.md P3 all [x]; KIMI_REVIEW_SUMMARY.md updated (P3 closed 32-34,
  whole 8.3-8.9 plan realized; open non-eval items: DI container, rate
  limiting /api/chat, semantic arg validation, unquoted JSON); README rating
  line. Waiting for external re-evaluation (9.5/10 claim).

UPDATE (2026-08-08) - non-eval important items done (stages 35-37), 1f8413f, 102/102 x2:
- Stage 35 rate limiting /api/chat: per-IP sliding window (AI_RATE_LIMIT,
  default 60 req/min) + concurrent in-flight burst cap (AI_RATE_BURST=6,
  decrement via task done-callback); 429 + retry seconds; AI_RATE_LIMIT=0
  disables; per-IP isolation. (LAN DoS item from external review 4.)
- Stage 36 semantic path validation: _path_dir_hint() in read/write/edit/
  patch - existing DIRECTORY -> 'Error: ... looks like a directory' with
  list/glob hint, BEFORE any mutation; read dir now Error-prefixed too.
- Stage 37 lenient JSON: unquoted string VALUES quoted incl. dotted/slash
  paths (tool: write, path: test.py) in _parse_tool_json.
- Tests +3 -> 102/102 x2; live: dir-hint via tools + server restarted,
  /health 200 (qwen3:8b).
- TODO.md: new section 'Вне оценок (закрыто 35-37)' + 'Осталось (10/10)':
  DI container, RAG/DB abstractions, Whisper server, external 9.5 re-eval.
- KIMI_REVIEW_SUMMARY.md/README updated. Awaiting re-evaluation claim 9.5.

UPDATE (2026-08-08) - Stage 38: EXTRA_ROOTS / ALLOW_OUTSIDE (outside-workspace support):
- Reason: user asked the :8765 agent to 'create a dating app in E:\test mycode';
  path-jail blocked it (folder outside workspace). Now:
- EXTRA_ROOTS='folder1;folder2' (env, also '|' separated, quotes stripped) -
  additional allowed roots; agent can read/write there without switching project.
- ALLOW_OUTSIDE=1 lifts the jail entirely (dangerous, local single-user only).
- Invented-path block (/path/to, /tmp, C:\Windows...) stays active regardless.
- ensure_safe_path error for absolute Windows paths now hints how to enable.
- backup()/git_auto_commit() already safe for external files (tested).
- Tests +1 (test_extra_roots) -> 103/103. LIVE: server restarted WITH
  EXTRA_ROOTS='E:\test mycode'; via /api/chat the agent created
  E:\test mycode\probe_live.txt (write confirmed 'yes') - works.
- Server currently running with EXTRA_ROOTS set (restart needed after reboot).

UPDATE (2026-08-09) - Stages 39-40: agent can now work OUTSIDE workspace on
request + trained to build SwiftMatch-class apps fully offline on Ollama:
- Stage 39 (glob outside workspace): _tool_glob accepts absolute patterns
  ('E:\proj\README.md', 'E:\proj\**\*.json') and cwd=<absolute folder>;
  jail still enforced (EXTRA_ROOTS/ALLOW_OUTSIDE). Reason: user asked the
  :8765 agent to study E:\swiftmatch1bdnoutprod - old glob only matched
  inside WORK_DIR ('No matches' on absolute patterns).
- Stage 40 (training for offline app-building, per 'Обучения программиста.txt'):
  - System prompts (tools/__init__.py SYSTEM_PROMPT + COMPACT_SYSTEM_PROMPT,
    tools/llm.py NATIVE_SYSTEM_PROMPT): new Rule 22 — folders outside the
    workspace => FULL absolute paths in every tool (read/write/edit/list,
    glob cwd=, bash cwd=); never fall back to workspace files; if 'outside
    workspace' -> ask user for EXTRA_ROOTS; call `skill` webapp before
    creating/extending web apps.
  - New skills in .agent_skills/ (methodology from 'Обучения программиста.txt'):
    webapp (main, chains siblings), swiftmatch_arch (full reference summary:
    stack/versions from package.json, 22 routers, WS events, 53 tables,
    security patterns, offline replacements), dating-app-auth (JWT+refresh+
    dev-login+rate-limit), dating-app-websocket (Socket.IO auth, user:{id}
    rooms, WebRTC signalling, TTL cleanup, typing/read-receipts events),
    dating-app-payments (Stripe+idempotency+webhook raw body+gating+mock
    offline mode), dating-app-geo (POINT SRID 4326, ST_Distance_Sphere,
    spatial index, offline fallback), dating-app-admin (adminAuth, ban/unban,
    feature_flags, moderation escalation, audit), dating-app-architecture
    (layers, soft delete, audit log, request-id, graceful shutdown),
    react-patterns (api.ts JWT client, RHF+zod, swipe framer-motion, chat
    socket hook, i18n), generate-api (few-shot module template + checklist),
    offline-ollama (replacements table: Redis->Map, Bull->setInterval,
    Stripe->mock, OpenAI mod->banned-words, S3->uploads/, Sentry->winston,
    Twilio->console; run on Laragon/MySQL; stage-by-stage workflow).
- Tests +1 (test_glob_outside_workspace) -> 104/104 x1; live: agent :8765
  answered 'какими скиллами создашь dating-приложение' by calling skill
  webapp + skill offline-ollama itself - training works.
- Server running with EXTRA_ROOTS='E:\swiftmatch1bdnoutprod;E:\test mycode'
  (restart needed after reboot).

UPDATE (2026-08-09) - Stages 41-43: remaining 10/10 items done:
- Stage 41 (DI container): core/container.py — register/resolve/has/reset +
  helpers (work_dir/sessions_dir/logger); agent.py registers live providers
  (work_dir, sessions_dir, memory_dir, logger, sessions_db) at startup;
  api_files/api_misc/api_sessions no longer `import agent as _agent` for
  runtime state (only plain functions remain). Providers are callables so
  switch_project() keeps working.
- Stage 42 (storage abstractions): core/abstractions.py — RAGStore protocol
  + RagAdapter (delegates to rag module) + KVStore protocol + SqliteKVStore
  (thread-safe, row_factory=Row); init_defaults() registers 'rag' in the DI
  container; 'sessions_db' registered by agent.py (lazy SqliteKVStore over
  DB_PATH). Swap-in points for FAISS→external vector DB / SQLite→Redis.
- Stage 43 (Whisper STT server, optional): stt.py — POST /api/stt
  (multipart 'file', ext whitelist wav/mp3/ogg/m4a/webm, max 25MB) + GET
  /api/stt/status; backends: AI_STT_URL (proxy to whisper server /inference
  returning {"text":...}) or AI_STT_BINARY (whisper.cpp main.exe: -m model
  -f audio -np 1 -nt 1 -l ru -otxt); neither → 501 with hint. UI static/app.js:
  no SpeechRecognition API → MediaRecorder fallback records → POST /api/stt →
  transcript into textarea; mic button hidden only if neither API exists.
- Tests +3 (test_di_container, test_abstractions, test_stt_endpoint) -> 107/107
  x2. pip: added python-multipart (needed for FastAPI form parsing).
- TODO.md 'Осталось (10/10)': DI, RAG/DB abstractions, Whisper server — all
  closed; only external 9.5 re-evaluation remains (needs a reviewer).
- Server restarted and LIVE-verified: /health ok, /api/stt/status answers
  {"url":false,"binary":false}.

UPDATE (2026-08-09) - Stage 44: re-evaluation package for external 9.5:
- RE_EVAL.md created: full state (107/107 x2 + live 3/3 qwen3:8b), what was
  closed after 8.9 (stages 35-43 table), 5-min verify steps (clone -> tests ->
  run :8765 -> skills check), expectations. TODO.md points to it.
- Live run reconfirmed: test_live.py --models qwen3:8b -> 3/3 (create file
  18.3s, simple question, edit rename 11.4s).

UPDATE (2026-08-09) - stages 45-48: agent development roadmap (111/111 x2):
- Stage 45 (reviewer/fixer subagents): tools/__init__.py SUBAGENT_PROMPTS —
  REVIEWER_PROMPT (multi-step code review: read files, list findings with
  file:line, examples, only concrete issues) and FIXER_PROMPT (apply fixes
  one by one, verify each, test before claiming done); GENERAL_PROMPT (big
  tasks split into steps). task tool proxies them through run_agent_loop.
  Test: test_reviewer_subagent.
- Stage 46 (RAG extra roots): rag.py — AI_EXTRA_RAG='E:\a;E:\b' adds folders
  OUTSIDE the workspace to the RAG index (keys E0/, E1/...); _file_root()
  resolves per-file root (workspace vs extra dir), _scan_files() includes
  extra roots; init_rag re-reads the env. Test: test_rag_extra_roots.
- Stage 47 (step budget): core/agent_loop.py — AGENT_STEP_BUDGET=N: on
  iteration N the loop FORCES a final plain-text summary (no tools) instead
  of spinning to AGENT_MAX_ITER. Test: test_step_budget (Deps must provide
  NO_CONFIRM/MAX_TOKENS; events=[] is falsy — stream path needs truthy
  callback, call_ollama path used in tests).
- Stage 48 (benchmark harness): test_bench.py — 6 scenarios (create-file,
  edit-rename, find-and-fix, js-create, sql-schema, refactor-extract), each
  = goal + verify callbacks; JSON report bench_reports/<model>.json with
  per-scenario pass/time. Test: test_bench_report.
- Test fixes while going 109/111 -> 111/111: test_rag_cache_incremental now
  joins the background RAG thread and snapshots/restores RAG state (the
  TestClient tests start the app -> a bg index of the real workspace
  polluted RAG_CHUNKS with add.py); test_step_budget needed NO_CONFIRM +
  MAX_TOKENS on Deps and the fake model must return a parseable tool block
  first (call_ollama path fires when events is falsy).
- Server restarted; EXTRA_ROOTS for E:\swiftmatch1bdnoutprod + AI_EXTRA_RAG
  available for reference-driven app building offline.

UPDATE (2026-08-09) - stages 49-51: live-verified new features + polish (111/111):
- Stage 49 (benchmark first real run): python -X utf8 test_bench.py --models
  qwen3:8b -> 6/6 (468s total; create-file 133.5s, edit-rename 9.7s,
  find-and-fix 41.5s, js-create 11.3s, sql-schema 132.0s, refactor-extract
  140.3s). Report: bench_reports/qwen3-8b.json. sql-schema checker relaxed:
  model wrote a correct inline FK (REFERENCES users(id)) — now accepts
  "FOREIGN KEY" OR "REFERENCES".
- Stage 50 (live checks of 45-47 on real qwen3:8b): (45) reviewer subagent
  reviewed calc.py -> "CRITICAL: calc.py:2 (division by zero), VERDICT: FAIL"
  in 23.5s (list -> read -> verdict); (46) AI_EXTRA_RAG=E:\swiftmatch1bdnoutprod
  search "jwt auth middleware" -> E0/eslint.config.js + calc.py in results
  (E0/ keys live); (47) AGENT_STEP_BUDGET=2 -> "[tool: BUDGET ...]" marker +
  forced plain-text summary in 25.8s, no extra tools. NOTE for test scripts:
  tools WORK_DIR must be set via init_config(WORK_DIR=...) — direct
  st.WORK_DIR= does NOT propagate to submodule copies (stage 14 lesson).
- Stage 51 (skill generate-api SQL section): rules for schema generation
  (users/messages + FK in both forms, ENGINE=InnoDB / PRAGMA foreign_keys,
  indexes on FKs, NNN_*.sql migrations) + full example from the bench
  sql-schema scenario.
- RE_EVAL.md updated: 111/111 x2 + live 3/3 + bench 6/6, stages 35-48 table,
  verify step 6 (benchmark run).
- TODO.md: stages 49-51 closed; 'Доделать' now: external 9.5 re-eval,
  benchmark other models (qwen2.5-coder:7b, 16b), live @fixer check.

UPDATE (2026-08-09) - stages 52-53: benchmark all models + live fixer (111/111):
- Stage 52 (benchmark other models): qwen2.5-coder:7b -> 1/6 (48.6s; answers
  with instructions instead of executing, only sql-schema passed);
  deepseek-coder-v2:16b -> 0/6 (105.6s; no tool execution at all). Verdict:
  qwen3:8b (native) is the only model that completes the full coding cycle
  on this stack. bench_reports/SUMMARY.md added (comparison table).
- Stage 53 (live fixer subagent): FIXER_PROMPT through run_agent_loop on
  qwen3:8b - division-by-zero bug in calc.py was actually FIXED (added
  b == 0 -> return None guard, function name and print kept), 60.4s.
- Tests 111/111, no code changes (docs/reports only this round).

UPDATE (2026-08-09) - stages 54-56: direct subagent markers in chat (113/113):
- Stage 54 (@reviewer/@fixer/@general markers): agent.py _apply_subagent_marker
  + _SUBAGENT_MARKER regex: first user token "@reviewer <task>" swaps msgs[0]
  system prompt for SUBAGENT_PROMPTS[type] and replaces the last user message
  content with the stripped task text (returns cleaned first_text so
  pick_task_model sees the real task). Wired into /api/chat after first_text
  extraction. No marker -> flow unchanged.
- Stage 55 (rules in prompts): SYSTEM_PROMPT rule 23 (review via
  task(agent='reviewer'), then fix with task(agent='fixer') - never patch
  manually after a report); COMPACT_SYSTEM_PROMPT short line; NATIVE_SYSTEM_PROMPT
  rule 17 (also mentions @-markers). test_system_prompt_rules still passes.
- Stage 56 (UI hints): static/app.js @-autocomplete now lists
  reviewer/fixer/general/explore/scout with label "run as subagent".
- Tests 113/113 (added test_subagent_marker: reviewer/fixer/general,
  punctuation/newline after marker, bare marker, no-marker passthrough;
  test_subagent_prompts_defined).

UPDATE (2026-08-09) - stages 57-59: subagent UI chips + live @fixer (113/113):
- Stage 57 (UI chips): ui.py adds three chip buttons (Ревью/Фикс/Агент) above
  the textarea, static/app.js subChip(type) inserts "@reviewer "/"@fixer "/
  "@general " at the cursor; placeholder now mentions the markers.
- Stage 58 (docs): USER_GUIDE.md "Как задавать задачи" - direct markers
  @reviewer/@fixer/@general (VERDICT report format) vs @explore/@scout
  (model-decided task tool).
- Stage 59 (live @fixer via marker): HTTP POST /api/chat with session_id and
  "@fixer Исправь баг деления на ноль в livefix/calc.py" -> fixer loop read
  the file, proposed edit (guard b == 0 -> return "Error: Division by zero
  is not allowed."), confirmed with a second "yes" message in the same
  session -> "Replaced in E:\My OpenCode1\livefix\calc.py, Syntax: OK".
  Note: leading-dot dirs (.test_tmp) confuse the model's edit path - use
  plain dir names for live subagent checks.

UPDATE (2026-08-09) - stages 60-62: subagent visibility + bench scenario (114/114):
- Stage 60 (status event): _apply_subagent_marker now returns (task, msgs,
  agent_type|None); /api/chat emits {type:status, msg:"subagent: <type>"} so
  the UI shows which subagent is running. test_subagent_marker updated.
- Stage 61 (/api/subagents): tools SUBAGENT_DESCS (short RU descriptions);
  GET /api/subagents returns name/marker/desc/tools-hint for all 5 subagents;
  test_subagents_api added.
- Stage 62 (bench scenario): test_bench.py "subagent-review" (7th) - model
  must call task(agent='reviewer') for bug2.py and report VERDICT.
- Tests 114/114.

UPDATE (2026-08-09) - stages 63-64: task tool reviewer/fixer fix + bench 7/7 (115/115):
- Stage 63 (bench 7 scenarios on qwen3:8b): first run 6/7 - subagent-review
  FAILED because the task tool rejected agent='reviewer' ("must be one of:
  explore, scout, general") even though SUBAGENT_PROMPTS has reviewer/fixer;
  the model degraded to general.
- Stage 64 (fix): tools/exec.py _tool_task + _validate_tool_block now accept
  reviewer/fixer; tool description in SYSTEM_PROMPT and native tools schema
  updated; test_task_subagent_reviewer_fixer added (both agents delegate with
  their prompts, unknown agent rejected). Live re-run: model called
  task(agent='reviewer') -> CRITICAL: .bench_tmp/bug2.py:3 ... VERDICT: FAIL.
  Final bench run: **7/7** (394.1s, subagent-review 38.9s), SUMMARY.md updated.
- Tests 115/115.

